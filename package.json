{
  "name": "claude-subagent-router",
  "version": "0.3.0",
  "private": true,
  "description": "Claude Code plugin that routes subagent work across Claude models and the Codex CLI.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "for f in scripts/*.mjs scripts/lib/*.mjs .claude/hooks/*.mjs; do [ -e \"$f\" ] || continue; node --check \"$f\" || exit 1; done"
  }
}
